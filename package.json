{
  "name": "kids-english-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Kids English Learning Agent Platform for Bé Nguyễn Ngọc Minh Anh",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"SERVER,CLIENT\" -c \"cyan,magenta\" \"node --watch server/src/index.js\" \"vite client\"",
    "dev:workspace": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run build && npm run start --workspace server",
    "check": "node --check server/src/index.js && node --check server/src/store.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "docx": "^9.7.1"
  }
}
